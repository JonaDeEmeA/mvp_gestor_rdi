rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Solo permite lectura y escritura a usuarios con sesión iniciada
      allow read, write: if request.auth != null;
    }
  }
}
